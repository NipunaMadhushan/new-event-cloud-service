# generated files
target/
generated/

# dependencies
Dependencies.toml

# config files
Config.toml
# the config files used for testing, Uncomment the following line if you want to commit the test config files
#!**/tests/Config.toml

# local tool caches
.kuberlr/

# Lambda build artifacts (rebuilt from source via npm install --production + zip
# per DEPLOYMENT_GUIDE.md §7 — never belong in version control)
lambda/*/node_modules/
lambda/*/function.zip
